const {
  buildIdentifier,
  buildVariableDeclaration,
  buildVariableDeclarator,
} = require('./ast-build');
const { difference } = require('./utils.js');

class Scope {
  constructor(parent) {
    // Write you code here
  }
  // Write the rest of methods and properties here
}

module.exports = Scope;